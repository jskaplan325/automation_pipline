id: ml-workspace
name: Azure Machine Learning Workspace
description: |
  A full-featured Azure Machine Learning workspace for data scientists and ML engineers to train, deploy, and manage machine learning models.

  **What's included:**
  - Azure ML Workspace with Studio access
  - Compute instances for development
  - Compute clusters for training (auto-scale)
  - Azure Container Registry for model images
  - Storage account for datasets and models
  - Key Vault for secrets
  - Application Insights for monitoring

  **Capabilities:**
  - Jupyter notebooks in the cloud
  - AutoML for automated model training
  - MLflow integration for experiment tracking
  - Model registry and versioning
  - Real-time and batch inference endpoints
  - Responsible AI dashboard

  **Best for:**
  Data science teams who need a complete MLOps platform for the full machine learning lifecycle, from experimentation to production deployment.

  **Getting Started:**
  Access ML Studio at ml.azure.com after deployment. Sample notebooks and a starter compute instance are included.

category: ai
icon: brain
skill_level: advanced
tags:
  - machine-learning
  - mlops
  - data-science
  - training
  - inference

estimated_monthly_cost_usd: 100-1500
cost_breakdown:
  - component: ML Workspace
    estimate: "Free"
  - component: Compute Instance
    estimate: "$50-300/month"
  - component: Training Cluster
    estimate: "$0-800/month"
  - component: Storage
    estimate: "$20-100/month"
  - component: Container Registry
    estimate: "$5-50/month"

parameters:
  - name: workspace_name
    label: Workspace Name
    type: string
    required: true
    description: Name for your ML workspace

  - name: size
    label: Environment Size
    type: select
    required: true
    options:
      - small
      - medium
      - large
    default: small
    description: |
      Small: 4 vCPU/16GB dev instance, 4-node CPU cluster (~$100/mo)
      Medium: 8 vCPU/32GB dev instance, 8-node CPU cluster (~$400/mo)
      Large: GPU dev instance (NC6), 8-node GPU cluster, premium storage (~$1500/mo)

  - name: region
    label: Azure Region
    type: select
    required: true
    options:
      - eastus
      - eastus2
      - westus2
      - westus3
      - northeurope
      - westeurope
    default: eastus

  - name: enable_gpu
    label: Enable GPU Compute
    type: boolean
    required: false
    default: "false"
    description: Add GPU capability for deep learning (included in Large size)

ado_pipeline:
  project: InfrastructureTeam
  pipeline_id: 55
  branch: main
